# Use KMS capture (bypasses broken XDG portal on Fedora Atomic/bootc)
capture = kms

# Run display setup scripts on client connect/disconnect
global_prep_cmd = [{"do":"/usr/local/bin/sunshine-do.sh","undo":"/usr/local/bin/sunshine-undo.sh"}]
